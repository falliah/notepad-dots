# Position status bar at the top
#set -g mouse on

#set -g status on

#set -g status-position top

#set -g status-style bg=default,fg=default

#set -g status-left ""
#set -s escape-time 0

#set -g mode-style bg=default,fg=default

# new
#set -g mode-style bg=default,fg=default

#set -g scroll-copy-mode-style ""
#set -g mode-line-format " "

#set -g window-status-format ""
#set -g window-status-current-format ""

# Put v Λ x on the top right
#set -g status-right "#[fg=default]v Λ x#[default]"
# Mouse & Timing
#set -g mouse on
#set -s escape-time 0

# Fix urxvt / X11 OSC color query leak (10;rgb text)
#set -as terminal-overrides ',rxvt*:OSC@:Ms@'
#set -g allow-passthrough off
#####
# Status Bar Position & Styling
#set -g status on
#set -g status-position top
#set -g status-style bg=default,fg=default

# Clear Left Side & Window List
#set -g status-left ""
#set -g window-status-format ""
#set -g window-status-current-format ""

# Right Side Text
#set -g status-right "#[fg=default]v Λ x#[default]"

# Copy / Scroll Mode Styling
#set -g mode-style bg=default,fg=default

# Disable tmux from requesting terminal colors on focus/start
#set -g default-terminal "screen-256color"
#set -s escape-time 0

# Turn OFF device attribute and color query capabilities
#set -ag terminal-overrides ",*:XT@:Cc@:Cr@:Cs@:Csr@:Ms@"
#set -as terminal-overrides ',*:Ms@:Cc@:Cr@:Cs@:Csr@:OSC@:TN@'
#set -g allow-passthrough off
#####


# Mouse support
set -g mouse on

# Instant escape response time (prevents query buffer leaks)
set -s escape-time 0

# Disable passthrough and color queries from rxvt
#set -g allow-passthrough off
#set -as terminal-features ',rxvt*:noosctitle:bcolor@'
#set -as terminal-overrides ',rxvt*:XT@:Ms@:Cc@:Cr@:Cs@:Csr@'

# Status bar position and base colors
set -g status-position top
set -g status-style bg=default,fg=default

# Clear left side and window list completely
set -g status-left ""
set -g window-status-format ""
set -g window-status-current-format ""

# Fixed right side text
set -g status-right "v Λ x"

# Remove copy-mode line counter and background highlight
set-window-option -g mode-style bg=default,fg=default
